export default defineAppConfig({
    ui: {
        primary: 'violet',
        grey: 'neutral',
    },
    nuxtIcon: {
        // Define properties for nuxtIcon here
        size: 'default',
        class: 'custom-icon-class',
        aliases: {
            // Define aliases if needed
        },
        iconifyApiOptions: {
            // Define iconify API options if needed
        }
    }
});
